## 🔰 프로젝트 인수인계 문서 (`hwajeon-main-web`)

> 화전마을관리 사회적협동조합 **메인 웹사이트 프론트엔드** 인수인계 문서입니다.  
> 저장소가 **공개(Public)** 일 수 있으므로 **키·토큰·서버 주소를 문서/이슈/채팅에 붙이지 않는 것**을 원칙으로 합니다.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **역할** | 조합 소개, 사업/카페 27b, 소식·참여, 문의, **관리자 게시 관리** |
| **기술** | React 19 + TypeScript + Vite 7, Tailwind, React Router 7, Radix/shadcn 패턴 |
| **백엔드** | 별도 저장소 **hawjeon** (Spring Boot). API 베이스 URL은 `VITE_API_BASE_URL` |
| **배포** | GitHub Actions (`prod` push) → 빌드 산출물을 서버 nginx 경로로 SCP |

---

## 2. 실행 및 빌드

- **Node.js**: 20 권장 (`.github/workflows/deploy.yml`과 동일)
- **로컬**:
  ```bash
  npm install
  cp .env.example .env   # 값은 팀 내 안전한 채널에서 공유
  npm run dev
  ```
- **빌드**: `npm run build` → `npm run preview`

---

## 3. 민감정보·보안 (공개 저장소 필수)

### 3.1 커밋하면 안 되는 것

- `.env`, `.env.local`, `.env.*.local` (이미 `.gitignore`에 포함)
- SSH 개인키, DB 비밀번호, AWS 시크릿, **실서비스 API 키 전체**
- 운영 서버 IP·계정명을 **불필요하게** 이슈/위키에 상시 기재

### 3.2 권장 패턴

| 목적 | 방법 |
|------|------|
| 신규 개발자 온보딩 | `.env.example`만 저장소에 두고, 실값은 1Password 등으로 전달 |
| 배포 | GitHub **Repository secrets** (`VITE_*`, `SERVER_*`, `SSH_PRIVATE_KEY` 등) |
| 코드 | `import.meta.env.VITE_*` 만 사용. 시크릿을 소스에 하드코딩하지 않음 |

### 3.3 `VITE_` 접두사에 대한 이해

Vite는 `VITE_`로 시작하는 변수만 클라이언트 번들에 노출합니다.  
**“노출되어도 괜찮은 값”만** 넣어야 합니다.

- 카카오 **JavaScript 키**·EmailJS **public key**는 원래 브라우저에 노출되는 용도이나, **남용 방지**를 위해 도메인 제한 등을 콘솔에서 설정하는 것이 좋습니다.
- **백엔드 JWT 서명용 시크릿**은 프론트에 두지 않습니다.  
  워크플로에 `VITE_JWT_SECRET`이 있어도 **현재 `src/`에서는 미사용**이므로, 혼동을 피하려면 Secrets/워크플로에서 제거 검토 가능합니다.

---

## 4. 디렉터리·정적 자산

### 4.1 `src/` (요약)

- `App.tsx` — 라우트·SEO·회원/관리자 레이아웃
- `api/` — `admin`(게시·인증), `member` API 래퍼
- `components/` — `ui/`, `layout/`, `news/`, `admin/` 등 도메인별
- `contexts/AuthContext.tsx` — 관리자 토큰·역할
- `lib/api.ts` — `apiClient`, 베이스 URL
- `pages/admin/Dashboard.tsx` — 게시 목록·검색(아래 7장 참고)

### 4.2 `public/` (빌드 시 `dist/` 루트로 복사)

SEO·크롤러용으로 **비밀 없이** 두어도 되는 파일:

- `robots.txt`, `sitemap.xml`, `rss.xml`
- `favicon.ico`, `logo.svg`

민감정보는 **여기에 두지 않습니다.**

---

## 5. 인증 (관리자)

- 로그인: `src/api/admin/auth.ts` → 토큰을 `localStorage` (`admin_token`, `refresh_token` 등)에 저장
- 보호 라우트: `ProtectedRoute` + `AuthContext`
- 상세 흐름은 이전 버전과 동일하며, 역할 상수(`TEACHER` 등) 변경 시 백엔드 JWT 페이로드와 맞출 것

---

## 6. 환경 변수 목록 (코드 기준)

| 변수 | 사용처 |
|------|--------|
| `VITE_API_BASE_URL` | `src/lib/api.ts` |
| `VITE_KAKAO_API` | `LocationMap.tsx` |
| `VITE_EMAILJS_SERVICE_ID` | 문의·봉사 폼 |
| `VITE_EMAILJS_TEMPLATE_ID` | 문의 등 |
| `VITE_EMAILJS_TEMPLATE_ID_VOLUNTEER` | `VolunteerApplicationModal` |
| `VITE_EMAILJS_PUBLIC_KEY` | EmailJS 초기화 |
| `VITE_EMAILJS_TO_EMAIL` | 수신 주소 |

배포 시 생성되는 `.env`는 **CI Secrets**로 채웁니다. 로컬은 `.env.example`을 복사해 사용합니다.

---

## 7. 알려진 동작 (관리자 대시보드 검색)

- 목록은 **서버 페이지네이션**(`postsApi.getPosts`의 `page`, `size`).
- 검색어는 **현재 페이지에 로드된 글만** `title`/`content`로 클라이언트 필터링합니다.
- 그래서 “전체 글 검색 + 페이지 일치”가 아니라, **페이지를 바꾸면 검색 대상 글 묶음도 바뀝니다.**  
  전체 검색이 필요하면 **백엔드에 `keyword` 쿼리 + DB 검색**을 두는 방식이 맞습니다.

---

## 8. 배포·CI/CD

- 워크플로: `.github/workflows/deploy.yml`
  - `pull_request` → `prod`: GitHub Pages 미리보기 `pr-<번호>/`
  - `push` → `prod`: 빌드 아티팩트 → `appleboy/scp-action`으로 서버 `/var/www/hwajeon_main_page`
- Secrets에 서버 SSH·`VITE_*` 등 필요

---

## 9. CORS·캐시 (운영 이슈)

### 9.1 증상

- `No 'Access-Control-Allow-Origin'`, `net::ERR_FAILED` (관리자/회원 API 호출 시)

### 9.2 원인 후보

1. 백엔드 `allowedOrigins`에 프론트 Origin 미등록
2. **배포된 백엔드 Docker 이미지가 구버전** (CORS 수정 미포함)
3. 브라우저 **캐시**로 옛 JS 사용 → 강력 새로고침 또는 시크릿 창으로 확인

### 9.3 서버에서 OPTIONS 확인 (예시)

```bash
curl -i -X OPTIONS 'https://api.example.com/homepage/category?postType=NEWS&page=0&size=1' \
  -H 'Origin: https://hi27b.com' \
  -H 'Access-Control-Request-Method: GET' \
  -H 'Access-Control-Request-Headers: authorization'
```

실제 API 도메인·Origin은 운영 기준으로 바꿉니다.

### 9.4 백엔드 배포

프론트만 수정해도 API 문제는 해결되지 않습니다. **hawjeon** 저장소의 `인수인계 문서.md`에 따라 `IMAGE_FULL_PATH`·ECR 태그를 확인합니다.

---

## 10. 인수인계 요약

1. **공개 저장소**: `.env` 실값·Secrets 내용을 커밋·복북 금지.  
2. **연동**: 백엔드 **hawjeon** + CORS + Docker 이미지 태그를 세트로 이해할 것.  
3. **검색**: 현재는 “현재 페이지 내 필터”. 전체 검색은 API 설계 필요.  
4. **문서**: `README.md`는 개발자용 요약, 본 문서는 운영·인수인계용 상세.

---

## 11. 관련 저장소

- **백엔드 (Spring Boot)**: `hawjeon` — `인수인계 문서.md`, `README.md`
