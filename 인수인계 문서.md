## 🔰 프로젝트 인수인계 문서 (`hwajeon-main-web`)

> 화전마을관리 사회적협동조합 메인 웹사이트 프론트엔드 인수인계용 문서입니다. 운영/개발자가 교체되더라도 서비스가 안정적으로 유지될 수 있도록 핵심 구조와 운영 포인트를 정리했습니다.

---

## 1. 프로젝트 개요

- **프로젝트명**: 화전마을관리 사회적협동조합 웹사이트
- **역할**: 조합 소개, 사업/카페 27b 안내, 소식/참여(회원·후원·자원봉사) 안내, 관리자용 게시 관리
- **기술 스택**
  - React 19 + TypeScript + Vite
  - React Router (회원/관리자 라우팅)
  - Tailwind CSS, shadcn 기반 UI 컴포넌트 (`src/components/ui`)
  - GitHub Actions 기반 CI/CD, 서버 배포(nginx) + GitHub Pages PR 미리보기

---

## 2. 실행 및 빌드 방법

- **필수 버전**
  - Node.js: 18 이상 (GitHub Actions에서는 20 사용)
  - npm: 9 이상

- **로컬 개발**
  1. 의존성 설치
     ```bash
     npm install
     ```
  2. 개발 서버 실행
     ```bash
     npm run dev
     ```
     기본적으로 Vite 개발 서버가 실행되며, SPA 라우팅을 사용합니다.

- **프로덕션 빌드**
  ```bash
  npm run build
  ```

- **프리뷰 (로컬 빌드 결과 확인)**
  ```bash
  npm run preview
  ```

---

## 3. 디렉터리 구조 및 역할

주요 디렉터리는 아래와 같습니다. 세부 구조는 `README.md`의 트리와 코드 주석을 참고하세요.

- `src/App.tsx`
  - 전체 라우팅과 레이아웃을 구성하는 엔트리 컴포넌트
  - `AuthProvider`, `ToastProvider`로 앱을 감싸고, 회원/관리자 라우팅을 모두 정의
  - 회원 페이지에는 `Header`, `Hero`, `Footer`를 포함하고, 관리자 페이지(`/admin`)에는 포함하지 않음

- `src/routes/MemberLayout.tsx`
  - `/member/*` 하위 페이지의 레이아웃 래퍼
  - 내부에서 `<Outlet />`만 감싸는 단순 레이아웃이므로, 필요 시 공통 UI(탭, 서브 내비게이션 등)를 여기서 확장 가능

- `src/components/`
  - `ui/`: 버튼, 인풋, 다이얼로그, 토스트 등 공통 UI 컴포넌트
  - `layout/`: `Header`, `Footer`, `SideBar` 등 레이아웃 관련 컴포넌트
  - `main/`: 메인 페이지 히어로, 섹션 컴포넌트
  - `business/`: 사업 안내 관련 도메인 컴포넌트
  - `combination/`: 조합 소개(연혁, 조직도, 인사말 등)
  - `news/`: 공지/행사/갤러리 등의 리스트/상세 컴포넌트
  - `participate/`: 회원가입, 후원, 자원봉사 등 참여 관련 컴포넌트
  - `contact/`: 문의/위치 정보, 지도 등
  - `auth/`: 관리자 인증 관련 컴포넌트(예: `ProtectedRoute`)

- `src/pages/`
  - **회원용 페이지**: `/member/*` 하위 URL과 1:1 매칭 (`Main`, `Combination`, `Business`, `Cafe27b`, `News`, `Participate`, `Contact`, `Journey`)
  - **관리자용 페이지**: `/admin/*` 하위 URL과 매칭 (`Login`, `Dashboard`, `CreatePost`, `EditPost`)

- `src/api/`
  - `admin/`
    - `auth.ts`: 관리자 로그인/로그아웃 API 래퍼 (`authApi`)
    - `index.ts`: `auth`, `posts` 등 관리자 API 통합 export

- `src/contexts/`
  - `AuthContext.tsx`: 관리자 인증 상태 관리 (로그인/로그아웃, 토큰, 사용자 정보)

- `src/hooks/`
  - `useApi.ts`: 공통 API 호출 로직(로딩/에러/데이터 상태 관리)
  - 기타 UI/상태 관련 커스텀 훅

- `src/lib/`
  - `api.ts`: `apiClient` 등 HTTP 클라이언트 설정(베이스 URL, 인터셉터 등) 정의
  - `utils.ts`: 공통 유틸 함수

- `src/types/`
  - API 응답, 컴포넌트 Props, 훅 반환 타입, UI/도메인 타입 정의

---

## 4. 라우팅 구조 (회원/관리자)

`src/App.tsx` 기준 주요 라우팅은 다음과 같습니다.

- **회원용(공개) 라우트**
  - `/` → `Main` 페이지
  - `/member` → `MemberLayout`
    - `/member/combination` → 조합 소개
    - `/member/journey/:step` → 조합 여정(스텝 단위)
    - `/member/business` → 사업 안내
    - `/member/cafe27b` → 카페 27b
    - `/member/news` → 소식/자료
    - `/member/participate` → 참여하기
    - `/member/contact` → 문의하기

- **관리자용 라우트**
  - `/admin/login` → 관리자 로그인 페이지
  - `/admin` → 관리자 레이아웃 (`AdminLayout`)
    - `/admin/dashboard` → 대시보드 (게시글 목록 등)
    - `/admin/create` → 게시글 생성
    - `/admin/edit/:id` → 게시글 수정
    - `/admin` 인덱스 → 디폴트로 `Dashboard`

> 관리자 라우트 보호는 `ProtectedRoute` 컴포넌트를 통해 구현되어 있으며, `AuthContext`의 `isAuthenticated` 및 `user.role` 값을 기준으로 접근 제어를 합니다.

---

## 5. 인증/권한 구조 (관리자)

### 5.1 토큰 기반 인증 흐름

- **로그인**
  - `src/api/admin/auth.ts`의 `authApi.login` 호출
  - 성공 시 응답에서 `accessToken`, `refreshToken`을 `localStorage`에 저장
    - 키: `admin_token`, `refresh_token`

- **`AuthContext`**
  - 파일: `src/contexts/AuthContext.tsx`
  - 앱 전체를 감싸는 `AuthProvider`에서:
    - 앱 최초 로드 시 `localStorage`의 `admin_token`을 확인하고, 존재하면 JWT 디코딩 후 `user`, `token` 상태를 설정
    - 토큰 디코딩 시 한글 깨짐을 감지하여, 문제 있을 경우 기본값 `'관리자'`로 대체
  - 제공 값:
    - `user`: `{ id, username, name, role, realName }`
    - `token`: 액세스 토큰
    - `isAuthenticated`: `!!user && !!token`
    - `login(credentials)`: 로그인 처리 후 성공 여부(boolean) 반환
    - `logout()`: API 로그아웃 시도 후, 로컬 상태와 `localStorage` 토큰 삭제

- **`useApi` 훅 (`src/hooks/useApi.ts`)**
  - API 호출 시 공통적으로 `loading`, `error`, `data` 상태를 관리
  - 백엔드에서 내려주는 에러 메시지를 우선 사용 (`getErrorMessage`)

### 5.2 라우트 보호

- 파일: `src/components/auth/ProtectedRoute.tsx`
  - `useAuth()`로 인증 상태를 읽어와 접근 제어
  - `isLoading`인 경우 풀스크린 로딩 스피너 표시
  - `isAuthenticated === false` → `/admin/login`으로 리다이렉트
  - `requireAdmin === true` + `user.role !== 'TEACHER'`인 경우 “접근 권한이 없습니다” 화면 표시

> 관리자 영역을 추가할 때는, 해당 페이지를 `ProtectedRoute`로 감싸고 `requireAdmin` 여부를 적절히 설정해야 합니다.

---

## 6. 환경 변수 및 백엔드 연동

### 6.1 Vite 환경 변수 사용

- GitHub Actions 워크플로(`.github/workflows/deploy.yml`)에서 다음 환경 변수를 설정하여 `.env`를 생성합니다.
  - `VITE_KAKAO_API` – 카카오 지도/SDK 키
  - `VITE_EMAILJS_SERVICE_ID`, `VITE_EMAILJS_TEMPLATE_ID`, `VITE_EMAILJS_PUBLIC_KEY`, `VITE_EMAILJS_TO_EMAIL` – EmailJS 설정값
  - `VITE_API_BASE_URL` – 백엔드 API 베이스 URL (예: `https://api.100youth.kr/`)
  - `VITE_JWT_SECRET` – JWT 관련 시크릿 (클라이언트 사용 여부는 코드 확인 필요)

- 이 값들은 Vite 규칙에 따라 `import.meta.env.VITE_*` 형식으로 코드에서 접근합니다.

### 6.2 `.env` 파일 관련 주의사항

- 레포지토리 루트의 `.env` 파일에는 현재 **백엔드 및 인프라 관련 민감 정보**(DB 계정, AWS 자격증명, JWT 시크릿 등)가 포함되어 있습니다.
  - **반드시 외부 유출 주의** (퍼블릭 저장소에 커밋 금지)
  - 로컬 개발에 꼭 필요한 값만 별도의 `.env.local` 등에 분리하고, 공용 저장소에는 예시용 `.env.example`만 두는 것을 권장합니다.

> 새 담당자는 `.env` 내용을 먼저 확인하고, 실제 운영 환경에서 사용하는 값과 로컬 개발에 필요한 값을 구분하여 관리 정책을 정비하는 것이 좋습니다.

---

## 7. 배포 및 CI/CD 파이프라인

### 7.1 GitHub Actions 워크플로 (`.github/workflows/deploy.yml`)

1. **PR 미리보기(preview job)**
   - 트리거: `prod` 브랜치 대상 `pull_request`
   - 동작:
     - 의존성 설치 후 `npm run build`
     - `dist` 결과를 GitHub Pages (`gh-pages` 브랜치) 하위 `pr-<번호>/` 디렉터리에 배포
     - PR에 미리보기 URL 코멘트 추가  
       예: `https://<owner>.github.io/<repo>/pr-<PR번호>/`

2. **프로덕션 빌드(package job)**
   - 트리거: `prod` 브랜치 `push`
   - 동작:
     - `.env` 파일 생성 (위에서 언급한 VITE_* 환경 변수들)
     - `npm run build` 로 프로덕션 빌드 생성
     - `dist` 디렉터리를 아티팩트로 업로드 (`dist-<commit_sha>`)

3. **서버 배포(deploy job)**
   - `package` 잡 이후 실행 (`needs: package`)
   - 아티팩트 `dist-<commit_sha>` 다운로드 후, `appleboy/scp-action`으로 운영 서버에 복사
     - 대상 경로: `/var/www/hwajeon_main_page`
     - SSH 접속 정보: `SERVER_IP`, `SERVER_USERNAME`, `SSH_PRIVATE_KEY` (GitHub Secrets)

> 실제 서비스 반영은 `prod` 브랜치에 머지/푸시될 때 자동으로 이루어집니다. 배포가 실패하면 GitHub Actions에서 로그를 반드시 확인해야 합니다.

### 7.2 Netlify 설정 (`netlify.toml`)

- 내용:
  ```toml
  [[redirects]]
    from = "/*"
    to = "/index.html"
    status = 200
  ```
- SPA 라우팅을 위한 404 → `index.html` 리다이렉트 설정입니다.
- 현재 주 배포는 GitHub Actions + 자체 서버(nginx) 기준이며, Netlify 설정은 예비/테스트용으로 보입니다.

---

## 8. 운영 시 주의사항 및 체크리스트

- **1) 민감 정보 관리**
  - `.env`에 포함된 DB 계정, AWS 키, JWT 시크릿 등은 외부 유출 시 큰 보안 사고로 이어질 수 있습니다.
  - 새 담당자는 `.gitignore` 및 시크릿 관리 정책을 재검토하고, 필요 시 키 회전(재발급)을 고려해야 합니다.

- **2) 백엔드 API 변경 시**
  - `src/lib/api.ts`의 베이스 URL 및 인터셉터, `src/api/admin/*`의 엔드포인트 정의를 함께 확인
  - 응답 스펙이 바뀌면 `src/types/api/*` 타입 정의와 `useApi` 에러 처리 로직을 업데이트해야 합니다.

- **3) 관리자 권한 체계 변경 시**
  - JWT 페이로드(특히 `role`) 구조가 변경되면:
    - `AuthContext`의 `decodeToken`
    - `ProtectedRoute`의 권한 체크(`user.role !== 'TEACHER'`)를 함께 수정해야 합니다.

- **4) 새 페이지/섹션 추가 시**
  - 라우트 추가: `src/App.tsx` (또는 적절한 `Layout` 하위 Route)
  - UI 컴포넌트: `src/components/<도메인>/`에 생성
  - 타입 정의: `src/types/components` / `src/types/api`에 추가

---

## 9. 인수인계 요약

- 이 프로젝트는 **React + TypeScript + Vite 기반의 SPA**로, 회원/관리자 두 가지 영역을 제공하며, 관리자 영역은 **JWT 기반 인증 + `AuthContext` + `ProtectedRoute`**로 보호됩니다.
- **배포는 GitHub Actions + nginx 서버** 조합으로 자동화되어 있으며, `prod` 브랜치로의 푸시가 곧 운영 반영입니다.
- `.env` 및 GitHub Secrets에 민감 정보와 API 설정이 집중되어 있으므로, **새 담당자는 먼저 환경 변수/시크릿 관리 전략을 확실히 파악**하는 것이 최우선입니다.
- 구조 자체는 도메인별로 잘 분리되어 있으므로, 새로운 기능 추가 시에는 **기존 디렉터리 구조와 타입 정의 패턴을 따라가는 것**을 권장합니다.

---

## 10. 최근 운영 이슈 인수인계 (2026-03)

### 10.1 증상

- 운영 사이트(`https://hi27b.com`)에서 뉴스/갤러리 상세 및 목록 조회 시 브라우저 콘솔에 아래 오류가 반복 발생:
  - `No 'Access-Control-Allow-Origin' header is present on the requested resource`
  - `Invalid CORS request`
- 사용자 화면에는 데이터가 비어 보이거나 "네트워크 연결을 확인해주세요." 오류가 표시됨.

### 10.2 원인

- 프론트 Origin이 `https://hi27b.com`으로 변경/운영되고 있었으나, 백엔드 CORS 허용 목록(`allowedOrigins`)에 해당 도메인이 미등록 상태였음.
- 초기 배포 단계에서 Docker 이미지 태그가 최신 코드가 아닌 구버전을 가리켜, CORS 수정이 반영되지 않은 이미지가 기동됨.
- 추가로 ECR 로그인 토큰 만료로 `docker compose pull`이 실패해, 운영자가 의도치 않게 기존 이미지로 재기동하는 상황이 발생함.

### 10.3 해결 과정 요약

1. 백엔드 CORS 허용 도메인에 `https://hi27b.com`, `https://www.hi27b.com` 추가
2. 수정 코드가 포함된 새 이미지 SHA로 ECR 배포
3. 서버 `~/app/.env`의 `IMAGE_FULL_PATH`를 해당 SHA로 교체
4. `docker compose up -d --no-deps --force-recreate hwajeon-app`로 강제 재생성
5. CORS 프리플라이트(OPTIONS) 응답 확인:
   - `Access-Control-Allow-Origin: https://hi27b.com`
   - HTTP 200 확인

### 10.4 운영 점검 명령어 (재발 시)

```bash
# 1) 현재 배포 이미지 확인
cd ~/app
grep IMAGE_FULL_PATH .env
sudo docker compose ps
CID=$(sudo docker compose ps -q hwajeon-app)
sudo docker inspect $CID --format '{{.Config.Image}}'

# 2) CORS 프리플라이트 점검 (백엔드 직접)
curl -i -X OPTIONS 'http://127.0.0.1:8081/homepage/category?postType=NEWS&page=0&size=1' \
  -H 'Origin: https://hi27b.com' \
  -H 'Access-Control-Request-Method: GET'

# 3) CORS 프리플라이트 점검 (실제 도메인 경유)
curl -i -X OPTIONS 'https://api.100youth.kr/homepage/category?postType=NEWS&page=0&size=1' \
  -H 'Origin: https://hi27b.com' \
  -H 'Access-Control-Request-Method: GET'
```

### 10.5 후속 권장사항

- 프론트 Origin 추가 시, 백엔드 CORS 허용 목록과 함께 변경하는 운영 체크리스트를 PR 템플릿에 포함
- 배포 후 smoke test 항목에 CORS OPTIONS 확인 추가
- API 도메인(`api.100youth.kr`)과 서비스 도메인(`hi27b.com`) 조합별 CORS 매트릭스를 문서화

